package com.company;
import java.util.Scanner;
import java.util.Arrays;
public class MathUtilExtended {
    public static void main(String[] args) {

//Task
        //
        //Ask the user to enter the sides of a right-angled triangle, a, b, c.
        //Find the area of the triangle.
        Scanner scanner = new Scanner(System.in);
        /*System.out.print("Enter the side a of the right-angled triangle: ");
        double a = scanner.nextDouble();
        System.out.print("Enter the side b of the right-angled triangle: ");
        double b = scanner.nextDouble();
        System.out.print("Enter the side c of the right-angled triangle: ");
        double c = scanner.nextDouble();
        double[] sides = {a, b, c};
        Arrays.sort (sides);
        if (Math.pow(sides[0],2) + Math.pow(sides[1],2) == Math.pow(sides[2],2)) {
            System.out.println("The area of the right-sided triangle is: " + (0.5 * sides[0] * sides[1]));
        }
        else
        {
            System.out.println("The triangle is not right-sided");
        }*/

//Task
        // Write a program to return the fibonacci sequence up to a specified length by the user
        // for example: how many fibonacci numbers do you want to display?


        /*System.out.print("How many fibonacci numbers do you want to display (minimum 2)?: ");
        int[] count = new int[scanner.nextInt()];
        int x = 0;
        int y = 1;
        System.out.println(x);
        System.out.println(y);
        for (int i = 2; i < count.length ; i++){
            int z = x+y;
            System.out.println(z);
            x = y;
            y = z;
        }*/






    }
}
