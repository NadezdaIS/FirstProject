package com.company;

public class StringBasic {
    public static void main(String[] args) {
    // A String is basically anything inside of double-quotes ("").
    // The index of the first character in a string is 0.
    // The index of the last character in a string is less than the length of the string by 1.

        /*String regularString = "I am going to the beach";
        int counter = 0;
           for (int i = 0; i < regularString.length(); i++) {
        if ('a' == regularString.toLowerCase().charAt(i)) {
            counter++;
            }
        }
        System.out.println("The number of times 'a' appeared is " + counter);*/
// Task
        // Write a program to count the number of times a vowel appears in a word.

       /*String firstWord = "Physician";
       String secondWord = "Aerodynamics";
       int counterFirst = 0;
        int counterSecond = 0;
        for (int i = 0; i < firstWord.length(); i++) {
            if ('a' == firstWord.toLowerCase().charAt(i) || 'o' == firstWord.toLowerCase().charAt(i)
            || 'e' == firstWord.toLowerCase().charAt(i) || 'i' == firstWord.toLowerCase().charAt(i)
            || 'u' == firstWord.toLowerCase().charAt(i)) {
                counterFirst++;
            }
        }
        for (int j = 0; j < secondWord.length(); j++) {
            if ('a' == secondWord.toLowerCase().charAt(j) || 'o' == secondWord.toLowerCase().charAt(j)
            || 'e' == secondWord.toLowerCase().charAt(j) || 'i' == secondWord.toLowerCase().charAt(j)
            || 'u' == secondWord.toLowerCase().charAt(j)) {
                counterSecond++;
            }
        }
        System.out.println("The number of vowels in the first word is " + counterFirst);
        System.out.println("The number of vowels in the second word is " + counterSecond);*/


    }
}

